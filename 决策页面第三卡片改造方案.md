# 设备数据决策页面 — 第三卡片改造方案

## 现状分析

当前决策页面顶部三栏卡片为：

| 卡片 | 内容 | 问题 |
|------|------|------|
| 灌溉控制 | 开关、出水量、时长、间隔 | ✅ 合理 |
| 阈值设置 | 湿度上下限、温度补偿、时段 | ✅ 合理 |
| **实时参数** | 土壤湿度/温度、空气温湿度、光照、累计灌溉 | ❌ 与实时数据页面重复 |

实时数据页面（RealTimeView）已通过 SSE 推送 + 轮询完整展示了 DHT11 和 device 两个设备的所有传感器数据，包含土壤湿度、温度等。决策页面不应再重复展示这些数据。

---

## 推荐方案（按推荐度排序）

### 方案一：智能决策建议 + 告警预警（推荐）

将第三卡片拆为两个区域，共用同一卡片或改为 2:1 布局。

**上半部分 — AI 决策建议摘要**：
- 当前灌溉建议（"建议立即灌溉" / "当前湿度充足，无需灌溉"）
- 预计下次灌溉时间
- 今日灌溉次数 / 累计用水量
- 调用 YYA 后端获取智能建议摘要（非对话形式，直接展示结论）

**下半部分 — 活跃告警**：
- 土壤湿度过低 / 过高告警
- 设备离线告警
- 异常数据告警（传感器数值突变）

**优点**：决策页面核心价值突出，一眼看到"该不该浇水"和"有什么问题"
**数据来源**：告警可从实时 SSE 数据带阈值判断；建议可调用 YYA 后端 `/api/chat/send` 获取结构化摘要

---

### 方案二：灌溉计划排程

以时间线/日程表形式展示当天的灌溉计划。

- 今日灌溉时间线（06:15 ✅ 已执行 / 12:00 ⏳ 待执行 / 18:00 📋 计划中）
- 下次灌溉倒计时
- 今日预计总灌溉量
- 快捷操作：跳过下次灌溉 / 立即执行

**优点**：直观的排程视图，对农场管理者最实用
**缺点**：需要后端支持定时灌溉计划逻辑

---

### 方案三：决策统计概览

以数据指标卡片形式展示决策相关的统计信息。

- 今日灌溉次数
- 今日累计用水量（升）
- 本月节水率（对比常规灌溉）
- 设备累计运行时长
- 决策执行成功率
- 近7天灌溉趋势迷你图

**优点**：对管理者有价值，容易实现
**缺点**：纯展示，不可操作；需要后端统计接口

---

### 方案四：快捷操作面板

提供一键式快捷操作按钮，简化常用决策动作。

- 🚿 立即灌溉（手动触发一次灌溉）
- ⏸️ 暂停今日计划
- 🔄 重置阈值为默认
- 📋 查看灌溉报告
- 🧠 咨询 AI 助手（直接唤起小沂对话框并带入当前场景）

**优点**：操作性强，降低使用门槛
**缺点**：需要后端支持指令下发

---

### 方案五：设备状态监控

展示灌溉相关硬件设备的实时运行状态。

- 水泵：运行中 / 待机 / 故障
- 电磁阀 1/2/3：开启 / 关闭
- 流量计读数
- 各传感器在线状态（在线 / 离线 / 数据超时）

**优点**：硬件运维视角，对现场管理者很重要
**缺点**：需要硬件上报设备状态数据

---

## 综合推荐

**最佳组合：方案一（智能建议 + 告警） + 方案四（快捷操作）合并**

将第三卡片改为两个区域：

```
┌─────────────────────────────────┐
│  🧠 智能决策                     │
│                                  │
│  💡 当前建议：土壤湿度偏低，      │
│     建议30分钟内启动灌溉          │
│                                  │
│  预计下次灌溉：今天 18:00         │
│  今日灌溉：2次 · 累计用水 45L    │
│                                  │
│  ⚠️ 活跃告警                     │
│  🔴 DHT11 土壤湿度过低 (28%)     │
│  🟡 设备 device 数据延迟 3分钟   │
├─────────────────────────────────┤
│  快捷操作                        │
│  [立即灌溉] [咨询AI] [查看报告]   │
└─────────────────────────────────┘
```

**理由**：
1. 智能建议直接回答"该做什么"，是决策页面的核心价值
2. 告警让问题不被遗漏
3. 快捷操作让决策能立即执行，形成闭环
4. 不依赖实时数据页面已有的传感器展示
5. 可渐进实现：先做建议+告警（数据可从前端实时数据推导），后续接入 YYA 后端获取更智能的建议

---

## 数据来源分析

| 数据项 | 来源 | 可行性 |
|--------|------|--------|
| 当前土壤湿度 | Pinia store（已有，SSE 推送） | ✅ 直接可用 |
| 灌溉建议结论 | 前端根据阈值 + 当前湿度判断 | ✅ 前端可直接计算 |
| 智能建议详情 | YYA `/api/chat/send` 携带上下文 | ✅ 后端已有接口 |
| 告警信息 | 前端根据阈值 + SSE 数据判断 | ✅ 前端可直接计算 |
| 快捷操作 | 后端需新增灌溉指令接口 | ⚠️ 需后端配合 |
| 灌溉统计 | 后端需新增统计接口 | ⚠️ 需后端配合 |

---

## 建议实施优先级

1. **第一阶段**（立即可做）：智能建议 + 告警（纯前端计算，利用已有 SSE 数据）
2. **第二阶段**（后端配合）：快捷操作面板（需后端灌溉指令接口）
3. **第三阶段**（后端配合）：历史统计图表（需后端统计接口）