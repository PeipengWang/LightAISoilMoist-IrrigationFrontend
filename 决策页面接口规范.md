# 智能决策建议模块 — 后端接口规范

## 概述

智能决策建议模块由 **YYA 智能体（端口 8001）** 驱动。前端将当前传感器数据和阈值配置发送给智能体，智能体分析后返回结构化决策建议（施肥、灌溉、危机预警等），前端按返回内容分条展示。

---

## 一、架构

```
┌──────────────────┐  SSE/轮询      ┌──────────────┐
│  后端 :8086      │ ─────────────→ │  Pinia Store  │ ──→ 灌溉控制 + 阈值设置
│  (已有接口)       │   latestData   │  devices.ts   │
└──────────────────┘                └──────────────┘

┌──────────────────┐  POST /api/decision/summary  ┌─────────────────┐
│  YYA 后端 :8001  │ ←──── { prompt } ─────────── │ DecisionView.vue │
│  (自行获取数据    │ ───────────────────────────→ │   结构化展示      │
│   调用智能体分析)  │  { summary, advices,        └─────────────────┘
│                  │    problematicSensors,
│   OneNET/数据库   │    alerts }
│   ↑ 自行拉取数据  │
└──────────────────┘
```

**关键原则**：分类建议（施肥/灌溉/危机）**有数据则展示，无数据则不展示**，由智能体决定返回哪些分类以及每条分类下的具体条目。

---

## 二、核心接口：POST /api/decision/summary

### 用途

前端发送固定提问指令，后端自行获取传感器数据与阈值配置，交由智能体分析后返回结构化决策建议。

### 请求

```
POST /api/decision/summary
Content-Type: application/json
```

```json
{
  "prompt": "请分析当前果园状况，给出今日农田管理建议"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `prompt` | string | ✅ | 发送给智能体的提问文本，后端可在此基础上拼接传感器上下文 |

> 前端不传传感器数据。后端收到请求后，自行从数据源（OneNET / 数据库）获取最新传感器数值和阈值配置，拼入 prompt 后调用智能体。

### 响应格式

```json
{
  "code": 0,
  "data": {
    "summary": "当前土壤湿度偏低，建议尽快灌溉15分钟；氮磷钾含量正常暂无需施肥；未来3天无极端天气预警。",
    "advices": [
      {
        "category": "irrigation",
        "icon": "🚿",
        "title": "灌溉建议",
        "level": "danger",
        "items": [
          "DHT11土壤湿度28%低于下限阈值30%，需立即灌溉",
          "device土壤湿度32%接近下限，建议同步灌溉",
          "建议灌溉时长15分钟，预计用水量约15L",
          "当前时段06:00-20:00在允许范围内"
        ]
      },
      {
        "category": "fertilization",
        "icon": "🌱",
        "title": "施肥建议",
        "level": "safe",
        "items": [
          "当前氮磷钾含量均在正常范围，暂不需要施肥",
          "预计下次施肥时间：7月15日（参考生长周期）"
        ]
      },
      {
        "category": "crisis",
        "icon": "⚠️",
        "title": "危机预警",
        "level": "info",
        "items": [
          "未来3天天气预报无极端高温或暴雨",
          "当前无病虫害风险"
        ]
      }
    ],
    "problematicSensors": [
      {
        "device": "DHT11",
        "name": "土壤湿度",
        "value": 28,
        "unit": "%",
        "status": "danger",
        "reason": "低于阈值30%"
      },
      {
        "device": "device",
        "name": "土壤温度",
        "value": 35,
        "unit": "°C",
        "status": "warning",
        "reason": "温度偏高，加速蒸发"
      }
    ],
    "alerts": [
      {
        "level": "danger",
        "device": "DHT11",
        "message": "土壤湿度过低（28%），已触发灌溉条件"
      },
      {
        "level": "warning",
        "device": "系统",
        "message": "数据连接存在3分钟延迟，决策可能滞后"
      }
    ]
  }
}
```

### 响应字段说明

#### data — 顶层

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `summary` | string | ✅ | 综合建议摘要（1-3句话），显示在卡片顶部 |
| `advices` | array | ✅ | 分项建议列表，**空数组表示无建议** |
| `problematicSensors` | array | ✅ | 异常传感器指标，**空数组表示全部正常** |
| `alerts` | array | ✅ | 告警信息，**空数组表示无告警** |

#### advices[] — 分项建议

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `category` | string | ✅ | 分类标识：`irrigation` / `fertilization` / `crisis` / `pest` / `weather` / 自定义 |
| `icon` | string | ✅ | 展示图标（emoji），如 `🚿` `🌱` `⚠️` `🐛` `🌤️` |
| `title` | string | ✅ | 分类标题，如"灌溉建议"、"施肥建议"、"危机预警" |
| `level` | string | ✅ | `danger` — 需处理 / `warning` — 需关注 / `safe` — 正常 / `info` — 信息 |
| `items` | string[] | ✅ | 建议条目列表，每条一个 `<li>`，**空数组则整个分类不展示** |

> 智能体根据实际分析结果决定返回哪些分类。例如无施肥需求时可只返回 `irrigation` 和 `crisis`，不返回 `fertilization`。前端按返回的 `advices[]` 逐条渲染，不为空才展示。

#### problematicSensors[] — 异常传感器

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `device` | string | ✅ | 设备名 |
| `name` | string | ✅ | 指标名 |
| `value` | number | ✅ | 当前值 |
| `unit` | string | ✅ | 单位 |
| `status` | string | ✅ | `danger` / `warning` |
| `reason` | string | ✅ | 异常原因简述，如"低于阈值30%" |

> 只返回有问题的传感器指标。全正常则返回空数组 `[]`，前端展示"全部指标正常"。

#### alerts[] — 告警信息

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `level` | string | ✅ | `danger` / `warning` / `info` |
| `device` | string | ✅ | 关联设备，系统级填 `"系统"` |
| `message` | string | ✅ | 告警描述 |

> 建议最多 5 条，按优先级排序。

---

## 三、前端展示逻辑

```
┌─ 🧠 小沂智能决策建议 ──── [需处理] ─┐
│                                      │
│  ┌─ 综合建议摘要 ──────────────────┐ │
│  │ 🔴 当前土壤湿度偏低，建议尽快... │ │
│  └────────────────────────────────┘ │
│                                      │
│  ┌─ 🚿 灌溉建议 ────── [需处理] ──┐ │
│  │ · DHT11土壤湿度28%低于阈值30%   │ │
│  │ · 建议灌溉时长15分钟...         │ │
│  └────────────────────────────────┘ │
│                                      │
│  ┌─ 🌱 施肥建议 ────── [正常] ────┐ │
│  │ · 氮磷钾含量正常，暂无需施肥    │ │
│  └────────────────────────────────┘ │
│                                      │
│  ┌─ ⚠️ 危机预警 ────── [信息] ────┐ │
│  │ · 未来3天无极端天气预警         │ │
│  └────────────────────────────────┘ │
│                                      │
│  ── ⚠️ 异常传感器指标 ──            │
│  [DHT11 土壤湿度 28% 低于阈值30%]   │
│  [device 土壤温度 35°C 温度偏高]    │
│                                      │
│  ── 🚨 告警信息 ──                  │
│  🔴 DHT11 土壤湿度过低              │
│  🟡 系统 数据连接3分钟延迟          │
└──────────────────────────────────────┘
```

**显示规则**：
- `summary` 始终显示（数据为空时显示"等待传感器数据..."）
- `advices[]` 逐条渲染，某条 `items` 为空数组则不显示该分类
- `problematicSensors` 为空 → 显示正常的传感器指标快照；不为空 → 标红/标黄展示
- `alerts` 为空 → 显示"✅ 当前无告警"

---

## 四、智能体实现建议

建议在 YYA 后端增加 `/api/decision/summary` 路由，调用智能体时使用以下 system prompt 约束输出为 JSON：

```
你是一个苹果种植智能决策助手。根据传入的传感器数据和阈值配置，
分析当前果园状况并返回结构化 JSON。

要求：
1. summary：1-3句综合摘要
2. advices：按实际情况返回灌溉建议(irrigation)、施肥建议(fertilization)、
   危机预警(crisis)等分类，某分类无需建议则不返回
3. problematicSensors：列出所有异常的传感器指标
4. alerts：列出需要关注的告警

只返回 JSON，不要附加其他文字。
```

---

## 五、错误响应

```json
{
  "code": -1,
  "message": "具体错误描述"
}
```

| code | 含义 |
|------|------|
| `0` | 成功 |
| `-1` | 智能体分析失败 |
| `-2` | 请求参数错误 |

---

## 六、降级策略

前端内置了降级逻辑：当 YYA 智能体不可用时，自动使用本地阈值计算生成基础建议（仅包含灌溉建议和简单的阈值告警），保证页面不会白屏。
